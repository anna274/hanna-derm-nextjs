import styled from 'styled-components';

export const ImageContainer = styled.div`
  overflow: hidden;
  z-index: -1;
  position: relative;
`;

export const HomeWording = styled.div`
  position: absolute;
  left: 50%;
  top: 20%;
  z-index: 1;
  padding-right: 50px;
`;

export const HomeTitle = styled.h1`
  font-size: 40px;
  margin-bottom: 20px;
`
